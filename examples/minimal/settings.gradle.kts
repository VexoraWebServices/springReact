pluginManagement {
    repositories {
        mavenLocal()        // the io.springreact plugin (published locally for this demo)
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "minimal"
