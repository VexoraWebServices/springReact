spring.react.title=Todos (API-backed)
# Where the REST API lives. Defaults to this same app; point it at a remote API service
# and the UI keeps working unchanged.
todo.api.base-url=http://localhost:8080
